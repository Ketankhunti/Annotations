/*
import org.checkerframework.checker.nullness.qual.NonNull;

public class PluggableTypeExample {

    public static void main(String[] args) {

        @NonNull String str = "Hello";  // Pluggable type: str must never be null

        printLength(str);
    }

    public static void printLength(@NonNull String text) {
        System.out.println(text.length());
    }
}
*/
